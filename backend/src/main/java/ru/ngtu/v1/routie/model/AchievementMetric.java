package ru.ngtu.v1.routie.model;

/** Метрика прогресса достижения — соответствует полю в {@link UserProfile}. */
public enum AchievementMetric {
    ROUTES_COMPLETED,
    DISTANCE_METERS,
    LANDMARKS_VISITED,
    LEVEL
}
